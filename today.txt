My wsus server name is STSWSUS-01, it is a workgroup server, The certificate that we bind, its CN is patching.stspartner.com.

So how my wsusutil.exe configuressl <YourWSUSServerName> will be?

is it 
wsusutil.exe configuressl STSWSUS-01

or 

wsusutil.exe configuressl patching.stspartner.com.
